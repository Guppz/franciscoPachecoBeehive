/**
* @name app.js
* @file start of the javascript .
* @author Francisco Pacheco <fpachecop0@mail.com>
* @version 1.0.0
*/
window.addEventListener('load', init, false);

function init() {
	console.log('App running!');
	var appManager = new AppManager();
}